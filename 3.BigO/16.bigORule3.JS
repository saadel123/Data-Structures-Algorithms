function compressBoxesTwice(boxes, boxes2){
    boxes.forEach(function(boxes) {
    console. log( boxes ) ;
    },
    boxes2.forEach(function(boxes2) {
    console. log( boxes2 ) ;
    }
}
// In this case time complexity is O(a+b) becasue each loop has it's own boxes and we don't know their lenght